<% title "Projects" %>

<div class="row">
  <div class="span10">
    <h1>Projects</h1>
  </div>
  <div class="span2">
    <%= link_to "create a project", new_project_path, :class => "btn btn-primary", :title => "create a project" %>
  </div>
</div>

<div class="row">
  <div class="span12">
    <%= page_entries_info @projects, :model => 'project' %>
    <%= will_paginate @projects, :previous_label => '&lt; previous', :next_label => 'next &gt;' %>
  </div>
</div>

<div class="row">
  <div class="span12">
    <ul class="index index-tabs index-stacked">
      <% @projects.each do |e| %>
      <li>
        <%= link_to e.name, edit_project_path(e) %>
      </li>
      <% end %>
    </ul>
    <%= will_paginate @projects, :previous_label => '&lt; previous', :next_label => 'next &gt;' %>
  </div>
</div>
