var ENTRY = (function($) {
    var app = {}, $el;
    // Public functions
    // app.foo = function() {  };
    // Private functions
    function init() {
        
    }
    // Call the init function on load
    $(init);
    return app;
} (jQuery));